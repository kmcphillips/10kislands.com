<div class="home-part-2">
  <%= render "page/card", card: @cards.first, index: 1, hidden: true %>

  <% @cards.each_with_index do |card, index| %>
    <%= render "page/card", card: card, index: index %>
  <% end %>
</div>
